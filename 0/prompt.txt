A session-scoped Stop hook is now active with condition: "ok, ahora haz un examen de logica y qa segun las practicas del repostorio de https://github.com/Elvis-WDev/ExpressJS-agent-context, luego que halles las fallas arreglalas". Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do. The hook will block stopping until the condition holds. It auto-clears once the condition is met — do not tell the user to run `/goal clear` after success; that's only for clearing a goal early.